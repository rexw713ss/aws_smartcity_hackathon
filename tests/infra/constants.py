"""Placeholder values for offline synthesis.

The account id is constructed rather than written as a 12-digit literal, so the
no-hard-coded-account-id guard stays clean.
"""

PLACEHOLDER_ACCOUNT = "0" * 12
PLACEHOLDER_EMAIL = "budget-alerts@example.invalid"
PLACEHOLDER_REGION = "ap-northeast-1"
