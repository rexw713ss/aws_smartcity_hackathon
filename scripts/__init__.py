"""Operational and verification scripts for the AWS workstream.

This package marker exists so tests can import script modules
(``import scripts.aws_preflight``) under the ``pythonpath = ["."]``
setting in ``pyproject.toml``.
"""
