# Leave unset for the deterministic local CSV preview (no AI network calls).
# Set this when the backend copilot endpoints are implemented; restart Vite.
# Same-origin example: /api/v1
# Local backend example (requires backend CORS):
# VITE_COPILOT_API_BASE=http://127.0.0.1:8000/api/v1
#
# Never put model-provider credentials or other secrets in a VITE_ variable.
